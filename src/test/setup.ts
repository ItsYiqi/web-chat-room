// Runs before every test file.
// Imports @testing-library/jest-dom to extend Vitest's `expect` with
// DOM matchers like toBeInTheDocument(), toHaveStyle(), toBeDisabled(), etc.
import '@testing-library/jest-dom'
